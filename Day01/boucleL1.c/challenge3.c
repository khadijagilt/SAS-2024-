#include <stdio.h>

int main(){
    int n, s, i, k;
    printf("entrez le nomber entier :");
    scanf("%d", &n);
    n = 2 k + 1;
    for( i = 1 ; i<= n ; i ++){
       s = s + i;
    }
    printf("  la somme des n premiers nombres naturels est %d.\n", s);
}
