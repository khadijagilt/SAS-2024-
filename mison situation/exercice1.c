#include <stdio.h>

int main(){
    float base, hauteur, aire,perimetre;
    printf("entrez la base :");
    scanf("%f", base);
    printf("%f", hauteur);
aire = ( base * hauteur) / 2;
perimetre = 3 * base;
printf("laire de tringle est %f.\n perimetre de tringle est %f.", aire, perimetre);


}
